<!DOCTYPE html>
<html lang="es">
<head>
    <title><%= titulo %></title>
    <style>
        body { font-family: sans-serif; background-color: #f0f0f0; }
        ul { list-style-type: '✅ '; }
        li { background-color: #fff; padding: 8px; margin-bottom: 5px; border-radius: 4px; }
        .empty { color: #888; }
    </style>
</head>
<body>
    <h1>Hola, <%= usuario.nombre.toUpperCase() %>!</h1>
    <p>Este es un template renderizado con nuestra versión moderna de EJS.</p>

    <% if (tareas && tareas.length) { %>
        <h2>Tus tareas pendientes:</h2>
        <ul>
            <% tareas.forEach(tarea => { %>
                <li><%= tarea %></li>
            <% }) %>
        </ul>
    <% } else { %>
        <p class="empty">¡Felicidades, no tienes tareas pendientes!</p>
    <% } %>
      <ul>
        <% categories.forEach(category => { %>
            <li><%= category.name %></li>
            <% category.notifications.forEach(n => { %>
              <li><%= n.text %></li>
            <% }) %>
        <% }) %>
    </ul>
    <hr>
    <em>Renderizado el <%= new Date().toLocaleDateString('es-ES') %></em>
</body>
</html>